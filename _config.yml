#------------------------------------------------------------
# Values for this lesson.
#------------------------------------------------------------

# Which carpentry is this ("swc" or "dc")?
carpentry: "swc"

# Overall title for pages.
title: "HPC in a day"

# Contact email address.
email: lessons@software-carpentry.org

#------------------------------------------------------------
# Generic settings (should not need to change).
#------------------------------------------------------------

# What kind of thing is this ("workshop" or "lesson")?
kind: "lesson"

# Magic to make URLs resolve both locally and on GitHub.
# See https://help.github.com/articles/repository-metadata-on-github-pages/.
repository: <USERNAME>/<PROJECT>

# Sites.
amy_site: "https://amy.software-carpentry.org/workshops"
dc_site: "http://datacarpentry.org"
swc_github: "https://github.com/swcarpentry"
swc_site: "https://software-carpentry.org"
swc_pages: "https://swcarpentry.github.io"
template_repo: "https://github.com/swcarpentry/styles"
example_repo: "https://github.com/swcarpentry/lesson-example"
example_site: "https://swcarpentry.github.com/lesson-example"
workshop_repo: "https://github.com/swcarpentry/workshop-template"
workshop_site: "https://swcarpentry.github.io/workshop-template"
training_site: "https://swcarpentry.github.io/instructor-training"

# this is the scheduler to be used for the workshop
# possible values: lsf, slurm, pbs
workshop_scheduler: "pbs"
workshop_login_host: "lengau.chpc.ac.za"
workshop_shared_fast_filesystem: "/mnt/lustre/users"
python_modulename: "chpc/python/3.6.1_gcc-6.3.0"
python_env_module_command: "$ module load chpc/python/3.6.1_gcc-6.3.0\n"
python_version: "3.6.1"
kernprof_command: "kernprof"
gnu_parallel_env_module_command: "module load chpc/gnu/parallel-20180622"
# location on the login host of the canteen menu collection
canteen_menu_dir: "/home/student99/this_weeks_canteen_menus/"
# some extra options for the snippets

# add a trailing space if you use this
pbs_extra_qsub_params: "-P WCHPC -q R1676493 "

# Surveys.
pre_survey: "https://www.surveymonkey.com/r/swc_pre_workshop_v1?workshop_id="
post_survey: "https://www.surveymonkey.com/r/swc_post_workshop_v1?workshop_id="

# Start time in minutes (0 to be clock-independent, 540 to show a start at 09:00 am)
start_time: 0

# Specify that things in the episodes collection should be output.
collections:
  episodes:
    output: true
    permalink: /:path/
  extras:
    output: true

# Set the default layout for things in the episodes collection.
defaults:
  - values:
      root: ..
  - scope:
      path: ""
      type: episodes
    values:
      layout: episode

# Files and directories that are not to be copied.
exclude:
  - Makefile
  - bin

# Turn off built-in syntax highlighting.
highlighter: false
